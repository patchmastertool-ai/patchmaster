server {
    listen 80;
    server_name patchmaster.example.com;

    root /opt/patchmaster/frontend/dist;
    index index.html;

    # Proxy API requests to FastAPI backend
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Serve agent installers from backend static dir
    location /download/ {
        alias /opt/patchmaster/backend/static/;
        try_files $uri =404;
    }

    location /static/ {
        alias /opt/patchmaster/backend/static/;
        try_files $uri =404;
    }

    # React SPA — serve index.html for all other routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# SSL example (enable when certificates are present)
#server {
#    listen 443 ssl http2;
#    server_name patchmaster.example.com;
#
#    ssl_certificate     /etc/letsencrypt/live/patchmaster.example.com/fullchain.pem;
#    ssl_certificate_key /etc/letsencrypt/live/patchmaster.example.com/privkey.pem;
#    ssl_protocols       TLSv1.2 TLSv1.3;
#    ssl_ciphers         HIGH:!aNULL:!MD5;
#
#    root /opt/patchmaster/frontend/dist;
#    index index.html;
#
#    location /api/ {
#        proxy_pass http://127.0.0.1:8000;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
#
#    location /download/ {
#        alias /opt/patchmaster/backend/static/;
#        try_files $uri =404;
#    }
#
#    location /static/ {
#        alias /opt/patchmaster/backend/static/;
#        try_files $uri =404;
#    }
#
#    location / {
#        try_files $uri $uri/ /index.html;
#    }
#}
