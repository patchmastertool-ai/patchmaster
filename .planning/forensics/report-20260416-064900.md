# Forensic Report

**Generated:** 2026-04-16T06:49:00Z
**Problem:** Database file configuration not correct, failing during installation time

---

## Evidence Summary

### Git Activity
- **Last commit:** 2026-04-16 06:44:53 — "fix(build): exclude .git folders from package tarball"
- **Commits (last 30):** 30 commits from 2026-04-15 to 2026-04-16
- **Time span:** 2026-04-15 06:37 → 2026-04-16 06:44
- **Uncommitted changes:** YES — 78 files modified, 22 deleted, many untracked
- **Active worktrees:** 1 (main only)

### Planning State
- **Current milestone:** v2.5.0 (completed)
- **Current phase:** Phase 14 (completed), gap closure
- **Last session:** 2026-04-15T09:07:01.988Z
- **Blockers:** Installation failure on customer site

### Artifact Completeness
| Phase | PLAN | CONTEXT | RESEARCH | SUMMARY | VERIFICATION |
|-------|------|---------|----------|---------|--------------|
| 01-07 | - | - | - | - | Partial |
| 08-10 | - | - | - | - | Missing |
| 11 | ✅ | - | - | ✅ | ✅ |
| 12 | - | - | - | - | ✅ |
| 13 | - | - | - | - | - |
| 14-16 | - | - | - | - | - |

---

## Anomalies Detected

### Database Index Conflict — **HIGH Confidence**
**Evidence:** Installation log shows:
```
asyncpg.exceptions.DuplicateTableError: relation "ix_patch_repos_name" already exists
```

**Interpretation:** The PatchRepo model in `backend/models/db_models.py` has `unique=True` AND `index=True` on the `name` column (line 1152). SQLAlchemy creates an implicit index for `unique=True`, and the explicit `index=True` creates a duplicate. When `init_db()` runs with `checkfirst=True`, the table creation is skipped but index creation is NOT skipped for existing tables, causing the duplicate index error.

**Root Cause:** The fix commits (`fc4185b`, `2402c32`) were made to address this, but the current model still has both `unique=True` and `index=True` on the `name` column. The fix may have only addressed one part of the issue.

### Scope Drift — **HIGH Confidence**
**Evidence:** Git log shows repeated database-related fixes:
- `fc4185b` — "Use checkfirst=True in create_all to handle existing indexes"
- `2402c32` — "Remove duplicate index definition for PatchRepo.name"
- `6e6a2e1` — "Use separate _engine_is_mock variable"

**Interpretation:** Multiple fixes to database.py and db_models.py suggest the database configuration has been unstable. The fixes are being applied to the tarball but the customer may be installing an older version (2.0.17).

### Installation Failure — **CONFIRMED**
**Evidence:** Installation log from `.planning/installation.md` (lines 588-684) shows:
```
[+] Step 6/8: Setting up backend...
[+]   Initializing database schema...
Traceback (most recent call last):
  ...
sqlalchemy.exc.ProgrammingError: relation "ix_patch_repos_name" already exists
[!] Database initialization failed.
```

**Interpretation:** The installation fails at database schema initialization, leaving the system in a broken state.

---

## Root Cause Hypothesis

The database configuration issue stems from **duplicate index definition** in the SQLAlchemy model for `PatchRepo.name`. The column has both `unique=True` (which creates an implicit index) and `index=True` (which creates an explicit index), resulting in `ix_patch_repos_name` being defined twice. While fixes were committed to the codebase, the customer is likely installing an older tarball (v2.0.17) that predates these fixes.

---

## Recommended Actions

1. **Immediate fix for current codebase:**
   Remove `index=True` from the `name` column since `unique=True` already creates an implicit index:
   ```python
   name = Column(String(200), nullable=False, unique=True)  # Remove: , index=True
   ```

2. **Rebuild and redistribute:**
   After fixing, rebuild the tarball:
   ```bash
   make build-release
   ```
   
3. **Customer workaround:**
   For the customer's existing installation, drop the duplicate index:
   ```sql
   DROP INDEX IF EXISTS ix_patch_repos_name;
   ```

4. **Verify fix:**
   Check that the fix commits (fc4185b, 2402c32) are actually in the tarball being distributed.

---

## Files Affected

| File | Issue | Status |
|------|-------|--------|
| `backend/models/db_models.py:1152` | Duplicate index on PatchRepo.name | Needs fix |
| `backend/database.py:137` | init_db() with checkfirst=True | OK |
| `packaging/build-package.sh` | .git exclusion | Fixed (936cba3) |

---

*Report generated by `/gsd-forensics`. All paths redacted for portability.*
