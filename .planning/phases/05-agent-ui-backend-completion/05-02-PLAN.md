---
phase: "05"
plan: "02"
type: "execute"
wave: "1"
depends_on: []
files_modified: []
autonomous: false
requirements: [UI-003, UI-007, UI-009, UI-011, UI-012, UI-013, UI-014, UI-015, UI-016, UI-017]
user_setup: []

must_haves:
  truths:
    - "Search results return within 2 seconds for large datasets"
    - "Export generates files without failures"
    - "Theme switching works without page reload"
    - "Keyboard navigation complete throughout app"
    - "Tooltips display helpful information"
    - "Print stylesheet applied correctly"
    - "CSV exports with correct UTF-8 encoding"
    - "Mobile responsive on tablet and phone"
    - "ARIA labels present for screen readers"
    - "Filter state persists across navigation"
artifacts:
  - path: "frontend/src/api/search.js"
    provides: "Optimized search with GIN index"
    min_lines: 50
  - path: "frontend/src/ExportPage.jsx"
    provides: "Fixed export functionality"
    min_lines: 100
  - path: "frontend/src/App.jsx"
    provides: "Theme switching support"
    min_lines: 50
key_links:
  - from: "frontend/src/App.jsx"
    to: "frontend/src/components/ThemeProvider.jsx"
    via: "Context provider"
    pattern: "theme"
---

<objective>
Fix remaining UI issues: slow search, export, theme switching, keyboard nav, tooltips, print styles, CSV encoding, mobile responsive, accessibility, filter persistence.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@ISSUE_TRACKER.md

From frontend/src/:
- React components exist
- CSS modules and global styles
- API service layer
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Slow Search with GIN Index</name>
  <files>frontend/src/api/search.js, backend/api/search.py, backend/models/db_models.py</files>
  <action>
Optimize search performance:
1. Add GIN index for full-text search on relevant columns
2. Implement search result caching
3. Add debouncing to search input
4. Implement pagination for search results
5. Use database-level search instead of Python filtering
  </action>
  <verify>
<automated>curl "http://localhost:8080/api/search?q=test&limit=10"</automated>
  </verify>
  <done>Search returns within 2 seconds</done>
</task>

<task type="auto">
  <name>Task 2: Fix Export Failures</name>
  <files>frontend/src/ExportPage.jsx, backend/api/export.py</files>
  <action>
Fix export functionality:
1. Add proper error handling for export failures
2. Implement streaming export for large datasets
3. Add export progress indicator
4. Fix any memory issues with large exports
5. Ensure all data types export correctly
  </action>
  <verify>
<automated>curl "http://localhost:8080/api/export/test" -o /dev/null -w "%{http_code}"</automated>
  </verify>
  <done>Export generates files without errors</done>
</task>

<task type="auto">
  <name>Task 3: Implement Theme Switching</name>
  <files>frontend/src/App.jsx, frontend/src/components/ThemeProvider.jsx, frontend/src/App.css</files>
  <action>
Implement theme switching without page reload:
1. Create ThemeProvider context
2. Store theme preference in localStorage
3. Apply theme class to root element
4. Support light/dark themes
5. Persist preference across sessions
  </action>
  <verify>
<automated>grep -r "ThemeProvider" frontend/src/ | head -3</automated>
  </verify>
  <done>Theme switches without page reload</done>
</task>

<task type="auto">
  <name>Task 4: Complete Keyboard Navigation</name>
  <files>frontend/src/</files>
  <action>
Ensure complete keyboard navigation:
1. Add tabIndex to all interactive elements
2. Implement keyboard shortcuts (Escape to close modals)
3. Add focus visible styles
4. Ensure logical tab order
5. Add skip-to-content link
  </action>
  <verify>
<automated>grep -r "tabIndex" frontend/src/ | wc -l</automated>
  </verify>
  <done>All features accessible via keyboard</done>
</task>

<task type="auto">
  <name>Task 5: Add Tooltips</name>
  <files>frontend/src/</files>
  <action>
Add helpful tooltips:
1. Use existing tooltip library or CSS-only tooltips
2. Add tooltips to icons, abbreviations, technical terms
3. Ensure tooltips are accessible (no hover-only)
4. Position tooltips correctly
5. Add keyboard trigger option
  </action>
  <verify>
<automated>grep -r "title=" frontend/src/ | head -5</automated>
  </verify>
  <done>Tooltips display on all icons and abbreviations</done>
</task>

<task type="auto">
  <name>Task 6: Fix Print Styles</name>
  <files>frontend/src/App.css</files>
  <action>
Fix print stylesheet:
1. Add @media print styles
2. Hide navigation, buttons during print
3. Ensure data tables print correctly
4. Add page breaks for long tables
5. Optimize for black/white printing
  </action>
  <verify>
<automated>grep -r "@media print" frontend/src/App.css</automated>
  </verify>
  <done>Print preview shows clean layout</done>
</task>

<task type="auto">
  <name>Task 7: Fix CSV Encoding</name>
  <files>backend/api/export.py</files>
  <action>
Fix CSV encoding issues:
1. Ensure UTF-8 BOM for Excel compatibility
2. Escape special characters properly
3. Handle international characters
4. Use csv module with correct encoding
  </action>
  <verify>
<automated>python -c "import csv; print('UTF-8')"</automated>
  </verify>
  <done>CSV opens correctly in Excel</done>
</task>

<task type="auto">
  <name>Task 8: Fix Mobile Responsive</name>
  <files>frontend/src/App.css</files>
  <action>
Fix mobile responsiveness:
1. Add responsive breakpoints
2. Fix table horizontal scroll on mobile
3. Ensure buttons are touch-friendly (44px min)
4. Fix navigation collapse on mobile
5. Test on various screen sizes
  </action>
  <verify>
<automated>grep -r "media query" frontend/src/App.css | wc -l</automated>
  </verify>
  <done>App usable on tablet and phone</done>
</task>

<task type="auto">
  <name>Task 9: Add ARIA Labels</name>
  <files>frontend/src/</files>
  <action>
Add accessibility improvements:
1. Add aria-label to icon buttons
2. Add aria-describedby for complex widgets
3. Add role attributes where needed
4. Ensure form labels are associated
5. Add live regions for dynamic content
  </action>
  <verify>
<automated>grep -r "aria-" frontend/src/ | wc -l</automated>
  </verify>
  <done>Screen reader announces content correctly</done>
</task>

<task type="auto">
  <name>Task 10: Fix Filter Persistence</name>
  <files>frontend/src/</files>
  <action>
Fix filter state persistence:
1. Store filter state in URL parameters
2. Restore filters from URL on page load
3. Add "Clear filters" button
4. Sync filter state with URL
  </action>
  <verify>
<automated>grep -r "useSearchParams" frontend/src/ | head -3</automated>
  </verify>
  <done>Filters persist across page navigation</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 11: Human Verification</name>
  <files>[All Task 1-10 files]</files>
  <what-built>All UI fixes: search, export, themes, keyboard, tooltips, print, CSV, mobile, ARIA, filters</what-built>
  <how-to-verify>
1. Search: Test with large dataset, measure response time
2. Export: Generate export, verify file opens
3. Themes: Toggle light/dark, verify no reload
4. Keyboard: Tab through app, verify all reachable
5. Mobile: Test on phone/tablet viewport
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Search returns within 2 seconds
- [ ] Export generates correctly
- [ ] Theme switching works
- [ ] Keyboard navigation complete
- [ ] Tooltips display
- [ ] Print styles work
- [ ] CSV encoding correct
- [ ] Mobile responsive
- [ ] ARIA labels present
- [ ] Filters persist
</verification>

<success_criteria>
All 15 UI issues resolved
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-ui-backend-completion/05-02-SUMMARY.md`
</output>
