---
phase: "05"
plan: "01"
type: "execute"
wave: "1"
depends_on: []
files_modified: []
autonomous: false
requirements: [AGENT-002, AGENT-003, AGENT-004, BACK-018, BACK-019, BACK-020]
user_setup: []

must_haves:
  truths:
    - "Windows Agent installs successfully on Windows Server 2019+"
    - "Agent auto-upgrades when version mismatch detected"
    - "Agent memory usage stays below 200MB under normal load"
    - "Logging performance optimized (async, batching)"
    - "Timezone handling consistent throughout application"
    - "IPv6 addresses supported in host configuration"
artifacts:
  - path: "agent/setup.py"
    provides: "Windows installer configuration"
    min_lines: 100
  - path: "agent/windows_service/"
    provides: "Windows service integration"
    min_lines: 50
  - path: "backend/agent_manager.py"
    provides: "Agent version management"
    min_lines: 80
  - path: "backend/monitoring_manager.py"
    provides: "Memory monitoring"
    min_lines: 60
key_links:
  - from: "backend/agent_manager.py"
    to: "backend/api/agent_proxy.py"
    via: "version check API"
    pattern: "version"
---

<objective>
Fix Agent blockers and complete Backend issues for v2.1.0 release.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@ISSUE_TRACKER.md

From agent/ directory:
- Windows installer scripts exist
- Agent package structure exists

From backend/:
- Database models exist
- API patterns established
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Windows Agent Installation</name>
  <files>agent/setup.py, agent/windows_service/, agent/build-all.sh</files>
  <action>
Fix Windows Agent installation issues:
1. Review existing setup.py for Windows-specific installation problems
2. Fix any path handling for Windows (use os.path.join, pathlib)
3. Ensure proper Windows service registration
4. Fix any permission issues during installation
5. Test installation on Windows path
  </action>
  <verify>
<automated>python -c "import setup; print('OK')"</automated>
  </verify>
  <done>Windows Agent installs without errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement Agent Version Mismatch Handling</name>
  <files>backend/agent_manager.py, backend/api/agent_proxy.py</files>
  <action>
Implement agent version check and auto-upgrade:
1. Add version field to agent heartbeat
2. Compare agent version with server expected version
3. If mismatch, trigger upgrade workflow
4. Add /api/agent/upgrade endpoint
5. Implement download and restart mechanism
  </action>
  <verify>
<automated>python -c "from backend.agent_manager import check_agent_version; print('OK')"</automated>
  </verify>
  <done>Server detects version mismatch and initiates upgrade</done>
</task>

<task type="auto">
  <name>Task 3: Fix Agent Memory Leak</name>
  <files>agent/, backend/monitoring_manager.py</files>
  <action>
Fix agent memory leak:
1. Identify memory leak sources (use memory profiler if available)
2. Common issues: unbounded caches, event listeners not cleaned up, connection pools
3. Add memory limits to agent config
4. Implement periodic garbage collection
5. Add memory monitoring to heartbeat
6. Set reasonable limits (max 200MB under normal load)
  </action>
  <verify>
<automated>grep -r "resource.setrlimit" agent/ || grep -r "gc.collect" agent/</automated>
  </verify>
  <done>Agent memory stays under 200MB</done>
</task>

<task type="auto">
  <name>Task 4: Optimize Logging Performance</name>
  <files>backend/logging_config.py, backend/main.py</files>
  <action>
Optimize logging performance:
1. Use async logging handlers (AsyncIOHandler)
2. Batch log writes to reduce I/O
3. Implement log rotation
4. Use structured logging (JSON format)
5. Reduce log level verbosity in hot paths
  </action>
  <verify>
<automated>python -c "from backend.logging_config import setup_logging; print('OK')"</automated>
  </verify>
  <done>Logging doesn't block request processing</done>
</task>

<task type="auto">
  <name>Task 5: Complete Timezone Handling</name>
  <files>backend/timezone_utils.py, backend/api/</files>
  <action>
Complete timezone handling:
1. Verify timezone_utils.py covers all use cases
2. Ensure all datetime objects are timezone-aware
3. Store all times in UTC, display in user timezone
4. Add timezone to API responses
5. Handle DST transitions correctly
  </action>
  <verify>
<automated>python -c "from backend.timezone_utils import get_current_timezone; print('OK')"</automated>
  </verify>
  <done>All times display correctly in user's timezone</done>
</task>

<task type="auto">
  <name>Task 6: Complete IPv6 Support</name>
  <files>backend/models/db_models.py, backend/api/hosts_v2.py</files>
  <action>
Complete IPv6 support:
1. Verify IPv6 validation in Host model
2. Update database schema to support IPv6 addresses
3. Update API to accept and store IPv6
4. Test IPv6-only and dual-stack scenarios
5. Document IPv6 configuration
  </action>
  <verify>
<automated>python -c "import ipaddress; ipaddress.ip_address('2001:db8::1'); print('OK')"</automated>
  </verify>
  <done>IPv6 addresses accepted and stored correctly</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 7: Human Verification</name>
  <files>[All Task 1-6 files]</files>
  <what-built>Windows installer, version mismatch handling, memory monitoring, optimized logging, timezone utilities, IPv6 support</what-built>
  <how-to-verify>
1. Windows Agent: Run installer on Windows Server
2. Version mismatch: Start agent with wrong version, verify upgrade triggers
3. Memory: Run agent under load, monitor memory usage
4. Timezone: Create events in different timezones, verify display
5. IPv6: Add host with IPv6 address, verify connectivity
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Windows Agent installs successfully
- [ ] Version mismatch detected and upgrade initiated
- [ ] Memory stays under 200MB
- [ ] Logging is async and doesn't block
- [ ] Timezone handling consistent
- [ ] IPv6 addresses work
</verification>

<success_criteria>
All Agent blockers and Backend issues resolved
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-ui-backend-completion/05-01-SUMMARY.md`
</output>
