---
phase: "09"
plan: "01"
type: "execute"
wave: "1"
depends_on: []
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Solaris agent installs and manages packages"
    - "HP-UX agent installs and manages packages"
    - "AIX agent installs and manages packages"
    - "UI shows all three OS in hosts list"
    - "Detection logic works for all three"
artifacts:
  - path: "agent/solaris_manager.py"
    provides: "Solaris package management (IPS/pkg)"
    min_lines: 300
  - path: "agent/hpux_manager.py"
    provides: "HP-UX package management (swinstall/swmgr)"
    min_lines: 300
  - path: "agent/aix_manager.py"
    provides: "AIX package management (installp/nim)"
    min_lines: 300
  - path: "agent/agent.py"
    provides: "Updated with Solaris/HP-UX/AIX detection"
    min_lines: 50
  - path: "backend/api/register_v2.py"
    provides: "OS detection for new platforms"
    min_lines: 30
key_links: []
---

<objective>
Add support for Solaris, HP-UX, and AIX operating systems with full package management and UI integration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@agent/agent.py (existing package managers)

Current supported OS: Windows, Linux (apt, dnf, pacman, zypper, apk), FreeBSD
New to add: Solaris, HP-UX, AIX
</context>

<tasks>

<task type="auto">
  <name>Task 1: Solaris Package Manager</name>
  <files>agent/solaris_manager.py</files>
  <action>
Implement Solaris package manager using IPS (Image Packaging System):

1. Create SolarisManager class extending BasePackageManager
2. Implement list_installed() using `pkg list -i`
3. Implement list_upgradable() using `pkg list -u`
4. Implement refresh() using `pkg refresh`
5. Implement install() using `pkg install`
6. Implement remove() using `pkg uninstall`
7. Implement check_reboot() for kernel updates
8. Add OS detection: check for SunOS/solaris kernel

Reference: https://docs.oracle.com/cd/E23824_01/html/E23823/doc-archive/pkg.html
</action>
  <verify>
<Automated>python -c "from agent.solaris_manager import SolarisManager; m = SolarisManager(); print('OK')"</Automated>
</verify>
  <done>Solaris agent manages packages</done>
</task>

<task type="auto">
  <name>Task 2: HP-UX Package Manager</name>
  <files>agent/hpux_manager.py</files>
  <action>
Implement HP-UX package manager using swinstall/swmgr:

1. Create HP-UXManager class extending BasePackageManager
2. Implement list_installed() using `swlist`
3. Implement list_upgradable() using `swlist -x update_available=true`
4. Implement refresh() using `swagentctl -r` or depot scan
5. Implement install() using `swinstall`
6. Implement remove() using `swremove`
7. Implement check_reboot() for kernel patches
8. Add OS detection: check for HP-UX/hpux kernel

Reference: https://hp.com/go/swmgr
</action>
  <verify>
<Automated>python -c "from agent.hpux_manager import HPUXManager; m = HPUXManager(); print('OK')"</Automated>
</verify>
  <done>HP-UX agent manages packages</done>
</task>

<task type="auto">
  <name>Task 3: AIX Package Manager</name>
  <files>agent/aix_manager.py</files>
  <action>
Implement AIX package manager using installp/NIM:

1. Create AIXManager class extending BasePackageManager
2. Implement list_installed() using `lslpp -L -qc all`
3. Implement list_upgradable() using `instfix -d | emgr -l`
4. Implement refresh() using `nimclient -o sync`
5. Implement install() using `installp` or NIM
6. Implement remove() using `installp -u`
7. Implement check_reboot() for mlpkgs
8. Add OS detection: check for AIX kernel

Reference: https://ibm.com/aix/posal
</action>
  <verify>
<Automated>python -c "from agent.aix_manager import AIXManager; m = AIXManager(); print('OK')"</Automated>
</verify>
  <done>AIX agent manages packages</done>
</task>

<task type="auto">
  <name>Task 4: Update Agent Detection</name>
  <files>agent/agent.py</files>
  <action>
Update agent/agent.py to detect and use new managers:

1. Add get_solaris_manager() function
2. Add get_hpux_manager() function
3. Add get_aix_manager() function
4. Update detect_package_manager() to check for new OS:
   - Check /usr/sbin/unixd (Solaris)
   - Check /usr/sbin/swagentd (HP-UX)  
   - Check /usr/bin/nimclient (AIX)
5. Import all three new managers in agent.py
</action>
  <verify>
<Automated>from agent.agent import detect_package_manager; print('OK')</Automated>
</verify>
  <done>New OS detected correctly</done>
</task>

<task type="auto">
  <name>Task 5: Backend OS Support</name>
  <files>backend/api/register_v2.py, backend/api/provisioning.py</files>
  <action>
Update backend to recognize new operating systems:

1. Add os_family: "solaris" to register_v2.py
2. Add os_family: "hpux" to register_v2.py
3. Add os_family: "aix" to register_v2.py
4. Update detect_os_family() in provisioning.py
5. Add appropriate icons/labels for UI
6. Add to hosts_v2.py table if needed
</action>
  <verify>
<Automated>from api.register_v2 import detect_os_family; print('OK')</Automated>
  </verify>
  <done>Backend recognizes new OS families</done>
</task>

<task type="auto">
  <name>Task 6: UI Integration</name>
  <files>frontend/src/</files>
  <action>
Update frontend to show new OS types:

1. Add Solaris to OS dropdown/filter
2. Add HP-UX to OS dropdown/filter
3. Add AIX to OS dropdown/filter
4. Add appropriate icons for each OS
5. Add OS-specific badge colors
6. Ensure filtering works for all three
</action>
  <verify>
<Automated>grep -r "solaris\|hpux\|aix" frontend/src/ | head -3</Automated>
  </verify>
  <done>UI shows all new OS types</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 7: Human Verification</name>
  <files>[All Task 1-6 files]</files>
  <what-built>Solaris, HP-UX, and AIX support</what-built>
  <how-to-verify>
1. Test agent import: python -c "from agent.solaris_manager import SolarisManager"
2. Test HP-UX: python -c "from agent.hpux_manager import HP-UXManager"
3. Test AIX: python -c "from agent.aix_manager import AIXManager"
4. Check backend: curl http://localhost:8080/api/hosts
5. Check UI: Visit hosts page, verify filters
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Solaris package manager working
- [ ] HP-UX package manager working
- [ ] AIX package manager working
- [ ] Agent detection works for all three
- [ ] Backend shows new OS families
- [ ] UI displays Solaris/HP-UX/AIX hosts
</verification>

<success_criteria>
All three UNIX platforms (Solaris, HP-UX, AIX) fully supported
</success_criteria>

<output>
After completion, create `.planning/phases/09-new-os-support/09-01-SUMMARY.md`
</output>