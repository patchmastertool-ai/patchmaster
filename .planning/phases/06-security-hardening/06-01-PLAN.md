---
phase: "06"
plan: "01"
type: "execute"
wave: "1"
depends_on: []
files_modified: []
autonomous: false
requirements: [SEC-001, SEC-002, SEC-003, SEC-004, SEC-005, SEC-006]
user_setup: []

must_haves:
  truths:
    - "All API endpoints require authentication"
    - "Input validation prevents XSS and SQL injection"
    - "Session tokens expire after inactivity timeout"
    - "Rate limiting protects abuse"
    - "RBAC restricts access to authorized roles"
    - "Security events are logged"
artifacts:
  - path: "backend/auth/security.py"
    provides: "Authentication and session management"
    min_lines: 100
  - path: "backend/auth/rbac.py"
    provides: "Role-based access control"
    min_lines: 80
  - path: "backend/middleware/rate_limit.py"
    provides: "API rate limiting"
    min_lines: 50
  - path: "backend/middleware/security.py"
    provides: "Security headers and CORS"
    min_lines: 40
key_links:
  - from: "backend/auth/security.py"
    to: "backend/api/"
    via: "authentication middleware"
    pattern: "require_auth"
---

<objective>
Implement security hardening: authentication improvements, input validation, session management, API security, RBAC, and audit logging.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@ISSUE_TRACKER.md

From backend/:
- Existing API patterns
- Existing authentication code
- Middleware structure
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve Authentication</name>
  <files>backend/auth/security.py, backend/api/auth.py</files>
  <action>
Improve authentication:
  1. Add token refresh mechanism
  2. Implement secure token generation ( secrets)
  3. Add MFA support structure
  4. Secure password hashing (bcrypt)
  5. Add login attempt rate limiting
  </action>
  <verify>
<automated>python -c "from backend.auth.security import create_token, verify_token; print('OK')"</automated>
  </verify>
  <done>Secure token creation and verification working</done>
</task>

<task type="auto">
  <name>Task 2: Input Validation</name>
  <files>backend/validators.py, backend/api/</files>
  <action>
Implement input validation:
  1. Create input validation utilities
  2. Sanitize string inputs (XSS prevention)
  3. Parameterized queries (SQL injection prevention)
  4. File upload validation
  5. API request size limits
  </action>
  <verify>
<automated>python -c "from backend.validators import sanitize_input, validate query; print('OK')"</automated>
  </verify>
  <done>All inputs validated and sanitized</done>
</task>

<task type="auto">
  <name>Task 3: Session Management</name>
  <files>backend/auth/security.py, backend/middleware/</files>
  <action>
Implement session management:
  1. Secure session cookies (HttpOnly, Secure, SameSite)
  2. Session timeout configuration
  3. Session invalidation on logout
  4. Concurrent session limits
  5. Refresh token rotation
  </action>
  <verify>
<automated>python -c "from backend.auth.security import create_session, validate_session; print('OK')"</automated>
  </verify>
  <done>Sessions expire after timeout</done>
</task>

<task type="auto">
  <name>Task 4: API Security Middleware</name>
  <files>backend/middleware/security.py, backend/middleware/rate_limit.py</files>
  <action>
Implement API security:
  1. Add security headers (HSTS, X-Frame-Options, CSP)
  2. Configure CORS properly
  3. Implement rate limiting per endpoint
  4. Add request ID for tracing
  5. Disable sensitive headers in responses
  </action>
  <verify>
<automated>python -c "from backend.middleware.security import add_security_headers; print('OK')"</automated>
  </verify>
  <done>API endpoints protected</done>
</task>

<task type="auto">
  <name>Task 5: RBAC Implementation</name>
  <files>backend/auth/rbac.py, backend/api/</files>
  <action>
Implement RBAC:
  1. Define roles and permissions
  2. Create role hierarchy
  3. Add permission check decorator
  4. Enforce RBAC on endpoints
  5. Add role assignment API
  </action>
  <verify>
<automated>python -c "from backend.auth.rbac import check_permission, has_role; print('OK')"</automated>
  </verify>
  <done>Access restricted by role</done>
</task>

<task type="auto">
  <name>Task 6: Security Audit Logging</name>
  <files>backend/audit.py, backend/auth/</files>
  <action>
Implement security audit logging:
  1. Log authentication attempts
  2. Log permission denials
  3. Log data access
  4. Log configuration changes
  5. Structured audit format
  </action>
  <verify>
<automated>python -c "from backend.audit import log_security_event; print('OK')"</automated>
  </verify>
  <done>Security events logged</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 7: Human Verification</name>
  <files>[All Task 1-6 files]</files>
  <what-built>Authentication, input validation, session management, API security, RBAC, audit logging</what-built>
  <how-to-verify>
1. Auth: Attempt login with wrong password, verify rate limiting
2. Input: Test XSS payload in search, verify sanitized
3. Session: Login, wait for timeout, verify logged out
4. API: Make rapid requests, verify rate limited
5. RBAC: Login as non-admin, verify access denied
6. Audit: Check logs for security events
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Authentication creates secure tokens
- [ ] Input validation prevents attacks
- [ ] Sessions expire after timeout
- [ ] Rate limiting active
- [ ] RBAC restricts access
- [ ] Security events logged
</verification>

<success_criteria>
All security hardening features implemented
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-hardening/06-01-SUMMARY.md`
</output>