---
phase: "08"
plan: "01"
type: "execute"
wave: "1"
depends_on: []
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Tests can collect without errors"
    - "Database tests use mocks or test database"
    - "Agent tests work in isolation"
    - "All existing functionality preserved"
artifacts:
  - path: "backend/tests/conftest.py"
    provides: "Pytest fixtures with mocks"
    min_lines: 50
key_links: []
---

<objective>
Fix test collection errors while preserving all existing functionality.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
Test errors found:
1. DATABASE_URL not set - causes sqlalchemy.exc.ArgumentError
2. IndentationError in license.py - FIXED
3. logger not defined before use - FIXED
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Test Fixtures</name>
  <files>backend/tests/conftest.py</files>
  <action>
Create pytest fixtures with mocks:
1. Mock DATABASE_URL for tests
2. Mock database sessions
3. Mock API imports
4. Ensure tests can run without real database
  </action>
  <verify>
<automated>python -m pytest backend/tests/ --collect-only 2>&1 | Select-Object -First 5</automated>
  </verify>
  <done>Tests collect without errors</done>
</task>

<task type="auto">
  <name>Task 2: Verify Agent Functionality</name>
  <files>agent/agent.py</files>
  <action>
Verify agent functionality preserved:
1. Test package detection works
2. Test snapshot creation
3. Test rollback capability
4. Test offline mode
  </action>
  <verify>
<automated>python -c "from agent import agent; print('OK')"</automated>
  </verify>
  <done>Agent functions work</done>
</task>

<task type="auto">
  <name>Task 3: Verify Backend Functionality</name>
  <files>backend/license.py</files>
  <action>
Verify backend functionality preserved:
1. License validation works
2. License caching works
3. Expiry warnings work
  </action>
  <verify>
<automated>python -c "from backend.license import get_license_info; print('OK')"</automated>
  </verify>
  <done>Backend functions work</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Human Verification</name>
  <files>[All Task 1-3 files]</files>
  <what-built>Test fixes with preserved functionality</what-built>
  <how-to-verify>
1. Run tests: pytest backend/tests/
2. Test agent: python -c "from agent import agent"
3. Test backend: python -c "from backend import main"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Tests collect without errors
- [ ] Agent functionality preserved
- [ ] Backend functionality preserved
</verification>

<success_criteria>
Test fixes applied without breaking existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-fixes/08-01-SUMMARY.md`
</output>