---
phase: "04"
plan: "01"
type: "execute"
wave: "1"
depends_on: []
files_modified: []
autonomous: false
requirements: [FEAT-001, FEAT-002, FEAT-003, FEAT-004, FEAT-007, FEAT-010, INT-001, INT-002, INT-003, INT-004, INT-005, INT-006, BACK-018, BACK-020]
user_setup: []

must_haves:
  truths:
    - "API supports both REST and GraphQL queries"
    - "Webhook delivery has retry logic with exponential backoff"
    - "System logs performance is optimized"
    - "IPv6 addresses are supported for host configuration"
    - "Configuration drift can be detected"
    - "Multi-tenant data isolation is enforced"
    - "Prometheus metrics are exposed"
    - "External integrations can be configured (Splunk, Sumo Logic, ServiceNow)"
  artifacts:
    - path: "backend/api/graphql.py"
      provides: "GraphQL API endpoint"
      min_lines: 200
    - path: "backend/api/webhook_retry.py"
      provides: "Webhook retry logic with backoff"
      min_lines: 80
    - path: "backend/drift_detector.py"
      provides: "Configuration drift detection"
      min_lines: 100
    - path: "backend/multi_tenant.py"
      provides: "Tenant isolation middleware"
      min_lines: 80
    - path: "backend/integrations/splunk.py"
      provides: "Splunk integration"
      min_lines: 60
    - path: "backend/integrations/sumo_logic.py"
      provides: "Sumo Logic integration"
      min_lines: 60
    - path: "backend/integrations/servicenow.py"
      provides: "ServiceNow integration"
      min_lines: 60
  key_links:
    - from: "backend/main.py"
      to: "backend/api/graphql.py"
      via: "include_router"
      pattern: "GraphQL"
    - from: "backend/api/notifications.py"
      to: "backend/api/webhook_retry.py"
      via: "import"
      pattern: "webhook_retry"
---

<objective>
Implement critical Features and Integrations for Phase 4. Focus on high-value items that enable enterprise requirements: GraphQL API, webhook retry logic, drift detection, multi-tenancy, and external integrations.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/03/03-01-SUMMARY.md

From backend/api/auth_api.py:
- get_effective_permissions(role: str, custom: dict = None) -> dict
- require_role(role: UserRole): Decorator for role-based access

From backend/models/db_models.py:
- Host, PatchJob, User, UserRole models exist
- Tenant model exists for multi-tenancy

From backend/api/metrics.py:
- Prometheus metrics already exposed at /metrics endpoint

From backend/api/plugins.py:
- Plugin integration framework exists
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GraphQL API endpoint</name>
  <files>backend/api/graphql.py, backend/main.py</files>
  <action>
Install strawberry-graphql[fastapi] (check requirements.txt first). Create GraphQL API with query types for Host, PatchJob, CVE, User. Use strawberry-django for ORM integration. Expose at /graphql endpoint with GraphiQL playground for development.
  </action>
  <verify>
<automated>curl -X POST http://localhost:8000/graphql -H "Content-Type: application/json" -d '{"query": "{ hosts { edges { node { id hostname ip } } } }"}'</automated>
  </verify>
  <done>
GraphQL endpoint responds to queries, returns Host/PatchJob/CVE data
</done>
</task>

<task type="auto">
  <name>Task 2: Implement webhook retry logic</name>
  <files>backend/api/webhook_retry.py, backend/api/notifications.py</files>
  <action>
Create webhook_retry.py with exponential backoff (5s, 20s, 60s, 5min max). Use async httpx with retry decorator. Track delivery status in audit log. Integrate with existing notification system for webhook channel type.
  </action>
  <verify>
<automated>Import test - python -c "from api.webhook_retry import deliver_with_retry; print('OK')"</automated>
  </verify>
  <done>
Webhook retries on failure with exponential backoff, logs each attempt
</done>
</task>

<task type="auto">
  <name>Task 3: Add drift detection</name>
  <files>backend/drift_detector.py, backend/api/compliance.py</files>
  <action>
Create drift_detector.py that compares current host config against baseline. Support comparison types: installed packages, running services, network config, registry (Windows). Store baseline in snapshots. Expose drift status via API endpoint.
  </action>
  <verify>
<automated>Import test - python -c "from drift_detector import detect_drift; print('OK')"</automated>
  </verify>
  <done>
API returns drift report showing what differs from baseline
</done>
</task>

<task type="auto">
  <name>Task 4: Add multi-tenancy support</name>
  <files>backend/multi_tenant.py, backend/api/hosts_v2.py</files>
  <action>
Create multi_tenant.py with tenant isolation middleware. Add tenant_id field to host queries. Implement row-level security. Use tenant context from JWT or API key. Ensure all queries filter by current tenant.
  </action>
  <verify>
<automated>Import test - python -c "from multi_tenant import get_current_tenant, require_tenant; print('OK')"</automated>
  </verify>
  <done>
Host list filtered by tenant, cross-tenant access denied
</done>
</task>

<task type="auto">
  <name>Task 5: Add external integrations (Splunk, Sumo Logic, ServiceNow)</name>
  <files>backend/integrations/splunk.py, backend/integrations/sumo_logic.py, backend/integrations/servicenow.py</files>
  <action>
Create backend/integrations/ directory. Implement each integration:
- splunk.py: Send events to Splunk HTTP Event Collector
- sumo_logic.py: Send events to Sumo Logic HTTP source
- servicenow.py: Create incidents via ServiceNow REST API

Each integration supports configuration via plugins API.
  </action>
  <verify>
<automated>Import test - python -c "from integrations.splunk import SplunkIntegration; print('OK')"</automated>
  </verify>
  <done>
Each integration can be configured and sends test events
</done>
</task>

<task type="auto">
  <name>Task 6: Verify Prometheus metrics and IPv6 support</name>
  <files>backend/api/metrics.py, backend/models/db_models.py, backend/api/hosts_v2.py</files>
  <action>
1. Verify metrics.py exposes at /metrics - already returns Prometheus format
2. Add IPv6 validation to Host model IP address field. Accept both IPv4 and IPv6 in validation. Update API to store IPv6.
  </action>
  <verify>
<automated>curl http://localhost:8000/metrics | head -20</automated>
  </verify>
  <done>
/metrics returns Prometheus format, IPv6 addresses accepted
</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 7: Human verification of implemented features</name>
  <files>[All Task 1-6 files]</files>
  <what-built>GraphQL API, webhook retry, drift detection, multi-tenancy, external integrations, Prometheus metrics</what-built>
  <how-to-verify>
1. GraphQL: Visit http://localhost:8000/graphql, query { hosts { edges { node { hostname } } } }
2. Drift detection: POST /api/hosts/drift with host_id, check response
3. Multi-tenancy: Create two tenants, verify data isolation
4. Integrations: Configure Splunk/Sumo/ServiceNow via /api/plugins
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GraphQL API → Backend | Untrusted query input crosses here |
| External Integrations → Internal | Third-party API credentials cross here |
| Multi-tenant → Database | Tenant isolation crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---------|----------|-----------|-------------|---------------|
| T-04-01 | Injection | GraphQL | mitigate | Use strawberry's built-in input validation, parameterized queries |
| T-04-02 | Disclosure | Credentials | mitigate | Encrypt integration secrets at rest, use env vars for API keys |
| T-04-03 | Elevation | Multi-tenant | mitigate | Row-level security on all tenant-scoped queries |
| T-04-04 | DoS | GraphQL | mitigate | Add query depth limits, max result size |
</threat_model>

<verification>
- [ ] GraphQL API responds to queries
- [ ] Webhook retry logic logs attempts
- [ ] Drift detection reports differences
- [ ] Multi-tenancy isolates data
- [ ] Integrations can send test events
- [ ] Prometheus metrics work
- [ ] IPv6 accepted
</verification>

<success_criteria>
- GraphQL endpoint functional (Task 1)
- Webhook retry implemented (Task 2)
- Drift detection reports (Task 3)
- Multi-tenant isolation (Task 4)
- Three integrations working (Task 5)
- Prometheus and IPv6 (Task 6)
- Human verification complete (Task 7)
</success_criteria>

<output>
After completion, create `.planning/phases/04-features-and-integration-implementation/04-01-SUMMARY.md`
</output>