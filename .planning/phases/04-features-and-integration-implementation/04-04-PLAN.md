---
phase: "04"
plan: "04"
type: "execute"
wave: "1"
depends_on: []
files_modified: [backend/main.py, backend/api/notifications.py, backend/api/policies.py, backend/api/compliance.py, backend/api/auth_api.py]
autonomous: true
gap_closure: true
requirements: [FEAT-003, FEAT-004, FEAT-005, FEAT-007, FEAT-010, INT-002]
user_setup: []

must_haves:
  truths:
    - "Rolling restart works in waves"
    - "Windows snapshots can be managed"
    - "Canary testing works"
    - "Multi-tenant data isolation is enforced"
    - "Webhook delivery has retry logic with exponential backoff"
    - "Configuration drift can be detected"
    - "RBAC controls are granular by feature"
    - "Patch dependencies can be resolved and applied in correct order"
  artifacts:
    - path: "backend/api/rolling_restart.py"
      provides: "Rolling restart API"
      min_lines: 486
      status: "wired"
    - path: "backend/api/windows_snapshot.py"
      provides: "Windows snapshot API"
      min_lines: 346
      status: "wired"
    - path: "backend/api/canary_testing.py"
      provides: "Canary testing API"
      min_lines: 536
      status: "wired"
    - path: "backend/multi_tenant.py"
      provides: "Tenant isolation middleware"
      min_lines: 250
      status: "wired"
  key_links:
    - from: "backend/main.py"
      to: "backend/api/rolling_restart.py"
      via: "include_router"
      status: "wired"
    - from: "backend/main.py"
      to: "backend/api/windows_snapshot.py"
      via: "include_router"
      status: "wired"
    - from: "backend/main.py"
      to: "backend/api/canary_testing.py"
      via: "include_router"
      status: "wired"
---

<objective>
Close 8 verification gaps from 04-VERIFICATION.md. All artifacts exist with substantial code but are not wired to the main application. This plan wires them up.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-features-and-integration-implementation/04-VERIFICATION.md

From backend/main.py (lines 33-86, router imports and registration pattern):
```python
from api.schedules import router as schedules_router
from api.audit import router as audit_router
...
app.include_router(schedules_router)
app.include_router(audit_router)
```

Pattern for wiring:
1. Import: `from api.<module> import router as <module>_router`
2. Register: `app.include_router(<module>_router)`

For middleware (multi_tenant):
```python
from multi_tenant import MultiTenantMiddleware
app.add_middleware(MultiTenantMiddleware)
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Rolling Restart API</name>
  <files>backend/main.py</files>
  <action>
Add import and router registration for rolling_restart.py:

1. After line 39 (schedules import), add:
   `from api.rolling_restart import router as rolling_restart_router`

2. After line 564 (schedules include_router), add:
   `app.include_router(rolling_restart_router)`

Reference existing router registration pattern from main.py lines 563-564.
  </action>
  <verify>
<automated>python -c "from backend.main import app; routes = [r.path for r in app.routes]; print('/api/rolling-restart/policies' in str(routes) or 'OK')"</automated>
  </verify>
  <done>Rolling restart API accessible at /api/rolling-restart/policies</done>
</task>

<task type="auto">
  <name>Task 2: Wire Windows Snapshot API</name>
  <files>backend/main.py</files>
  <action>
Add import and router registration for windows_snapshot.py:

1. Add import (after rolling_restart import):
   `from api.windows_snapshot import router as windows_snapshot_router`

2. Add router registration (after rolling_restart_router):
   `app.include_router(windows_snapshot_router)`
  </action>
  <verify>
<automated>python -c "from backend.main import app; routes = [r.path for r in app.routes]; print('OK')"</automated>
  </verify>
  <done>Windows snapshot API accessible</done>
</task>

<task type="auto">
  <name>Task 3: Wire Canary Testing API</name>
  <files>backend/main.py</files>
  <action>
Add import and router registration for canary_testing.py:

1. Add import (after windows_snapshot import):
   `from api.canary_testing import router as canary_testing_router`

2. Add router registration:
   `app.include_router(canary_testing_router)`
  </action>
  <verify>
<automated>python -c "from backend.main import app; print('OK')"</automated>
  </verify>
  <done>Canary testing API accessible</done>
</task>

<task type="auto">
  <name>Task 4: Wire Multi-Tenant Middleware</name>
  <files>backend/main.py</files>
  <action>
Add multi-tenant middleware to app:

1. Add import (after othermiddleware imports):
   `from multi_tenant import MultiTenantMiddleware`

2. Add middleware (after LicenseMiddleware registration, around line 513):
   `app.add_middleware(MultiTenantMiddleware)`

Note: Middleware order matters - add after LicenseMiddleware but before other middleware.
  </action>
  <verify>
<automated>python -c "from backend.main import app; print('OK')"</automated>
  </verify>
  <done>Multi-tenant middleware active for all requests</done>
</task>

<task type="auto">
  <name>Task 5: Integrate Webhook Retry into Notifications</name>
  <files>backend/api/notifications.py</files>
  <action>
Integrate webhook_retry.py into notifications.py webhook delivery path:

1. Read notifications.py to find webhook delivery function

2. Replace direct httpx calls with deliver_with_retry from webhook_retry.py:
   `from api.webhook_retry import deliver_with_retry`

3. Update webhook sending to use the retry wrapper instead of direct httpx.post

Reference: webhook_retry.py has deliver_with_retry(url, payload, ...) function with exponential backoff.
  </action>
  <verify>
<automated>python -c "from api.notifications import send_webhook; print('OK')"</automated>
  </verify>
  <done>Webhooks retry on failure with exponential backoff</done>
</task>

<task type="auto">
  <name>Task 6: Add Drift Detection API Endpoint</name>
  <files>backend/drift_detector.py, backend/main.py</files>
  <action>
Create API endpoint for drift detection:

Option A (preferred): Add FastAPI router to drift_detector.py
- Add: `from fastapi import APIRouter`
- Create: `router = APIRouter(prefix="/api/hosts", tags=["drift"])`
- Add endpoints: GET /drift, POST /drift/{host_id}
- Wire to main.py

Option B: Add to existing compliance.py
- Add drift-related endpoints to compliance router

Verify by checking drift_detector.py has the logic, just needs API exposure.
  </action>
  <verify>
<automated>python -c "from drift_detector import DriftDetector; print('OK')"</automated>
  </verify>
  <done>Drift detection API accessible at /api/hosts/drift</done>
</task>

<task type="auto">
  <name>Task 7: Wire RBAC require_permission Decorator</name>
  <files>backend/api/auth_api.py, backend/api/rbac.py</files>
  <action>
Wire RBAC require_permission into endpoints:

1. In rbac.py, ensure require_permission decorator is properly defined
   (should already exist with Permission enum)

2. Update auth_api.py to use rbac.py permissions:
   - Import: `from api.rbac import require_permission, Permission`
   - Replace or supplement require_role with require_permission

3. The decorator should check both role AND specific permissions
   before allowing access to protected endpoints.

Reference: rbac.py has require_permission decorator and Permission enum.
  </action>
  <verify>
<automated>python -c "from api.rbac import require_permission; print('OK')"</automated>
  </verify>
  <done>Endpoints check granular permissions</done>
</task>

<task type="auto">
  <name>Task 8: Integrate Dependency Resolver into Patch Jobs</name>
  <files>backend/api/policies.py, backend/api/dependency_resolver.py</files>
  <action>
Integrate dependency_resolver.py into patch job execution:

1. Import the resolver in policies.py:
   `from api.dependency_resolver import DependencyResolver, resolve_dependencies`

2. When creating/applying patch policies:
   - Call resolve_dependencies(patch_list) to get correct order
   - Apply patches in topologically sorted order
   - Handle circular dependencies with error

3. The resolver should be used during:
   - Policy creation (to validate dependencies)
   - Job execution (to order patches correctly)

Reference: dependency_resolver.py has DependencyResolver class with resolve_dependencies method.
  </action>
  <verify>
<automated>python -c "from api.dependency_resolver import resolve_dependencies; print('OK')"</automated>
  </verify>
  <done>Patches applied in correct dependency order</done>
</task>

</tasks>

<verification>
- [ ] Rolling restart API responds at /api/rolling-restart/policies
- [ ] Windows snapshot API accessible
- [ ] Canary testing API accessible
- [ ] Multi-tenant middleware enforced on all requests
- [ ] Webhook retry integrated into notifications
- [ ] Drift detection API accessible at /api/hosts/drift
- [ ] RBAC granular permissions checked on endpoints
- [ ] Dependencies resolved before patch application
</verification>

<success_criteria>
All 8 verification gaps from 04-VERIFICATION.md are closed:
1. rolling_restart.py wired ✓
2. windows_snapshot.py wired ✓
3. canary_testing.py wired ✓
4. multi_tenant.py middleware added ✓
5. webhook_retry.py integrated ✓
6. drift_detector.py has API endpoint ✓
7. rbac.py require_permission used ✓
8. dependency_resolver.py integrated ✓
</success_criteria>

<output>
After completion, create `.planning/phases/04-features-and-integration-implementation/04-04-SUMMARY.md`
</output>