---
phase: "04"
plan: "03"
type: "execute"
wave: "2"
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: false
requirements: [INT-007, INT-008, INT-009, BACK-019, FEAT-001, FEAT-002, FEAT-004]
user_setup: []

must_haves:
  truths:
    - "Jira integration can create/update tickets"
    - "Slack integration sends notifications"
    - "Custom integrations can be defined"
    - "Timezone handling is correct throughout"
    - "Rolling restart works in waves"
    - "Windows snapshots can be managed"
    - "Canary testing works"
  artifacts:
    - path: "backend/integrations/jira.py"
      provides: "Jira integration"
      min_lines: 80
    - path: "backend/integrations/slack.py"
      provides: "Slack integration"
      min_lines: 80
    - path: "backend/integrations/custom.py"
      provides: "Custom integration framework"
      min_lines: 60
    - path: "backend/timezone_utils.py"
      provides: "Timezone utilities"
      min_lines: 40
    - path: "backend/api/rolling_restart.py"
      provides: "Rolling restart API"
      min_lines: 60
    - path: "backend/api/windows_snapshot.py"
      provides: "Windows snapshot management"
      min_lines: 80
    - path: "backend/api/canary_testing.py"
      provides: "Canary testing API"
      min_lines: 60
  key_links:
    - from: "backend/integrations/jira.py"
      to: "backend/api/plugins.py"
      via: "Integration base class"
    - from: "backend/integrations/slack.py"
      to: "backend/api/notifications.py"
      via: "Notification channel"
---

<objective>
Implement Jira, Slack, and custom integrations. Fix timezone handling. Implement rolling restart, Windows snapshots, and canary testing using existing infrastructure.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

From backend/api/ring_rollout.py:
- Ring rollout policy supports canary (5% batch)

From backend/api/agent_proxy.py:
- Snapshot endpoints exist (/snapshot/create, /snapshot/rollback)

From backend/api/notifications.py:
- Slack webhook support exists
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Jira integration</name>
  <files>backend/integrations/jira.py, backend/api/plugins.py</files>
  <action>
Create backend/integrations/jira.py. Use Jira REST API (Cloud/Server). Support creating issues, updating status, adding comments. Map PatchMaster events to Jira workflow. Configure via plugins API.
  </action>
<verify>
<automated>Import test - python -c "from integrations.slack import SlackIntegration; print('OK')"</automated>
  </verify>
  <done>
Jira issues created for patch events
</done>
</task>

<task type="auto">
  <name>Task 2: Add Slack integration</name>
  <files>backend/integrations/slack.py, backend/api/notifications.py</files>
  <action>
Enhance existing Slack support. Create modular slack.py in integrations/. Support channels, DMs, threads. Add rich message formatting (blocks). Map events to Slack messages.
  </action>
  <verify>
<parameter name="verify">
<auto>Import test - python -c "from integrations.slack import SlackIntegration; print('OK')"</auto>