---
phase: "04"
plan: "02"
type: "execute"
wave: "1"
depends_on: []
files_modified: []
autonomous: false
requirements: [FEAT-003, FEAT-005, FEAT-006, FEAT-008, FEAT-009, FEAT-011, FEAT-012]
user_setup: []

must_haves:
  truths:
    - "Patch dependencies can be resolved and applied in correct order"
    - "RBAC controls are granular by feature"
    - "Scheduled patch windows can be created and enforced"
    - "Compliance reports can be generated"
    - "Audit log captures all system events"
    - "Plugin framework supports custom extensions"
    - "Custom reports can be built"
  artifacts:
    - path: "backend/api/dependency_resolver.py"
      provides: "Patch dependency resolution"
      min_lines: 80
    - path: "backend/api/rbac.py"
      provides: "Granular role-based access"
      min_lines: 60
    - path: "backend/api/compliance.py"
      provides: "Compliance report generation"
      min_lines: 100
    - path: "backend/api/audit.py"
      provides: "Audit logging"
      min_lines: 80
    - path: "backend/api/plugins.py"
      provides: "Plugin framework (already exists, enhance)"
      min_lines: 433
    - path: "backend/api/reports.py"
      provides: "Custom report builder"
      min_lines: 200
  key_links:
    - from: "backend/api/policies.py"
      to: "backend/api/dependency_resolver.py"
      via: "import"
      pattern: "dependency"
    - from: "backend/api/auth_api.py"
      to: "backend/api/rbac.py"
      via: "require_role"
---

<objective>
Implement remaining Features: dependency resolution, RBAC granularity, scheduling, compliance, audit, plugins, and custom reports. Many have partial implementations that need enhancement.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/03/03-01-SUMMARY.md

From backend/api/ring_rollout.py:
- Can implement rolling restart via existing ring_rollout policies with rings

From backend/api/schedules.py:
- Patch schedules already exist

From backend/api/compliance.py (exists):
- Compliance API exists, enhance with full reports

From backend/api/audit.py (exists):
- Audit API exists, enhance with full logging

From backend/api/plugins.py (exists):
- Plugin framework exists with webhook type

From backend/api/reports.py (exists):
- Reports API exists
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add patch dependency resolution</name>
  <files>backend/api/dependency_resolver.py, backend/api/policies.py</files>
  <action>
Create dependency_resolver.py that parses patch dependencies from metadata. Build dependency graph. Topological sort for installation order. Handle circular dependencies with error. Support "requires", "conflicts", "replaces" metadata.
  </action>
  <verify>
<automated>Import test - python -c "from api.dependency_resolver import resolve_dependencies; print('OK')"</automated>
  </verify>
  <done>
Patches applied in correct dependency order
</done>
</task>

<task type="auto">
  <name>Task 2: Enhance RBAC with granular permissions</name>
  <files>backend/api/rbac.py, backend/api/auth_api.py</files>
  <action>
Create rbac.py with granular permission controls. Define feature-level permissions (view/manage/execute). Add to existing roles. Expose permission management UI endpoints.
  </action>
  <verify>
<automated>Import test - python -c "from api.rbac import check_permission; print('OK')"</automated>
  </verify>
  <done>
Fine-grained access control per feature
</done>
</task>

<task type="auto">
  <name>Task 3: Verify/enhance compliance and audit</name>
  <files>backend/api/compliance.py, backend/api/audit.py</files>
  <action>
1. Compliance: Verify existing API generates reports. Add support for custom compliance templates, PCI-DSS, HIPAA, SOC2 frameworks.
2. Audit: Verify existing API captures all events. Add tamper-proof logging, log retention policies, search/export.
  </action>
  <verify>
<automated>curl http://localhost:8000/api/compliance/reports | head -5</automated>
  </verify>
  <done>
Compliance reports generated, audit logs search-able
</done>
</task>

<task type="auto">
  <name>Task 4: Enhance plugin framework</name>
  <files>backend/api/plugins.py</files>
  <action>
Enhance existing plugins.py. Add more plugin types (pre-install, post-install, pre-reboot, post-reboot hooks). Add plugin execution queue with retry. Add plugin development SDK documentation.
  </action>
  <verify>
<automated>Import test - python -c "from api.plugins import execute_plugin_hooks; print('OK')"</automated>
  </done>
  </verify>
  <done>
Custom hooks can be executed at patch lifecycle points
</done>
</task>

<task type="auto">
  <name>Task 5: Enhance custom reports</name>
  <files>backend/api/reports.py, frontend/src/ReportsOpsPage.jsx</files>
  <action>
Enhance existing reports.py. Add custom report builder with drag-drop fields. Add scheduled report delivery. Add report templates (executive summary, CVE breakdown, compliance status).
  </action>
  <verify>
<automated>curl http://localhost:8000/api/reports | head -5</automated>
  </verify>
  <done>
Custom reports can be created and scheduled
</done>
</task>

<task type="auto">
  <name>Task 6: Verify scheduling features</name>
  <files>backend/api/schedules.py</files>
  <action>
Verify existing schedules.py. Add maintenance window support. Add blackout periods. Add timezone-aware scheduling.
  </action>
  <verify>
<automated>curl http://localhost:8000/api/schedules | head -5</automated>
  </verify>
  <done>
Patch windows can be scheduled with timezone
</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 7: Human verification</name>
  <files>[All Task 1-6 files]</files>
  <what-built>Dependency resolution, RBAC, compliance, audit, plugins, reports, scheduling</what-built>
  <how-to-verify>
1. Create interdependent patches, verify order
2. Test role permissions
3. Generate compliance report for specific framework
4. Search audit logs
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin Execution → Host | Custom code execution crosses here |
| RBAC → Database | Permission changes cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---------|----------|-----------|-------------|-----------------|
| T-04-10 | Execution | Plugins | mitigate | Sandboxed plugin execution, timeout limits |
| T-04-11 | Tampering | Audit Log | mitigate | Write-only append, hash chain for integrity |
</threat_model>

<verification>
- [ ] Dependency resolution works
- [ ] RBAC has granular controls
- [ ] Compliance generates reports
- [ ] Audit captures events
- [ ] Plugin hooks execute
- [ ] Custom reports work
- [ ] Scheduling works
</verification>

<success_criteria>
All 7 remaining Features addressed (Partial implementations enhanced)
</success_criteria>

<output>
After completion, update SUMMARY.md
</output>