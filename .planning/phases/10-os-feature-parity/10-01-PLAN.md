---
phase: "10"
plan: "01"
type: "execute"
wave: "1"
depends_on: ["09"]
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "All three managers have same features as existing platforms"
    - "Security filtering works"
    - "Download packages works"
    - "Version info works"
artifacts:
  - path: "agent/solaris_manager.py"
    provides: "Solaris with all features"
    add_lines: 100
  - path: "agent/hpux_manager.py"
    provides: "HP-UX with all features"
    add_lines: 100
  - path: "agent/aix_manager.py"
    provides: "AIX with all features"
    add_lines: 100
key_links: []
---

<objective>
Add missing features to Solaris, HP-UX, and AIX managers to match all existing platform features.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
Existing managers in agent/agent.py have these methods NOT in new managers:
- get_proxies() - proxy handling
- download_packages() - offline package download
- _filter_security_packages() - CVE filtering via backend
- get_update_info() - detailed update info
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Security Filtering to All Managers</name>
  <files>agent/solaris_manager.py, agent/hpux_manager.py, agent/aix_manager.py</files>
  <action>
Add _filter_security_packages() method to each manager:
1. Query backend CVE API
2. Filter packages with known CVEs
3. Return security-relevant packages
Use same implementation as in BasePackageManager
  </action>
  <verify>
<automated>python -c "from agent.solaris_manager import SolarisManager; m=SolarisManager(); print(hasattr(m,'_filter_security_packages'))"</automated>
  </verify>
  <done>Security filtering available</done>
</task>

<task type="auto">
  <name>Task 2: Add Download Packages to All Managers</name>
  <files>agent/solaris_manager.py, agent/hpux_manager.py, agent/aix_manager.py</files>
  <action>
Add get_proxies() and download_packages() methods:
1. get_proxies() - return proxy settings from environment
2. download_packages() - download packages for offline install
3. Handle platform-specific download URLs
  </action>
  <verify>
<automated>python -c "from agent.solaris_manager import SolarisManager; m=SolarisManager(); print(hasattr(m,'download_packages'))"</automated>
  </verify>
  <done>Download packages works</done>
</task>

<task type="auto">
  <name>Task 3: Add Detailed Update Info</name>
  <files>agent/hpux_manager.py, agent/aix_manager.py</files>
  <action>
Add get_update_info() method (Solaris already has):
1. Get current package version
2. Get available version
3. Get publisher/source info
4. Determine if update available
  </action>
  <verify>
<automated>python -c "from agent.hpux_manager import HPUXManager; m=HPUXManager(); print(hasattr(m,'get_update_info'))"</automated>
  </verify>
  <done>Update info available</done>
</task>

<task type="auto">
  <name>Task 4: Add Version Info to HP-UX Manager</name>
  <files>agent/hpux_manager.py</files>
  <action>
Ensure HP-UX manager has version tracking:
1. Add version() method
2. Add get_current_version()
3. Add parse_version() helper
  </action>
  <verify>
<Automated>python -c "from agent.hpux_manager import HPUXManager; m=HPUXManager(); print('OK')"</Automated>
  </verify>
  <done>Version tracking works</done>
</task>

<task type="auto">
  <name>Task 5: Add Version Info to AIX Manager</name>
  <files>agent/aix_manager.py</files>
  <action>
Ensure AIX manager has version tracking:
1. Add version() method
2. Add get_current_version()
3. Add lslpp parsing for versions
  </action>
  <verify>
<Automated>python -c "from agent.aix_manager import AIXManager; m=AIXManager(); print('OK')"</Automated>
  </verify>
  <done>Version tracking works</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 6: Human Verification</name>
  <files>[All Task 1-5 files]</files>
  <what-built>All managers have same features</what-built>
  <how-to-verify>
1. python -c "from agent.solaris_manager import SolarisManager; print(dir(SolarisManager()))"
2. python -c "from agent.hpux_manager import HPUXManager; print(dir(HPUXManager()))"
3. python -c "from agent.aix_manager import AIXManager; print(dir(AIXManager()))"
4. Verify all methods present
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Security filtering works on all managers
- [ ] Download packages works on all managers
- [ ] Update info works on all managers
- [ ] Version tracking works on all managers
</verification>

<success_criteria>
All new OS managers have same features as existing ones
</success_criteria>

<output>
After completion, create `.planning/phases/10-os-feature-parity/10-01-SUMMARY.md`
</output>