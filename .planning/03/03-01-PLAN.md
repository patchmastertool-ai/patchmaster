---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.js
  - backend/api/hosts_v2.py
  - backend/api/dashboard.py
autonomous: true
requirements: [UI-004, UI-005, UI-006, UI-008, UI-010]

must_haves:
  truths:
    - "Charts render efficiently with large datasets (1000+ data points)"
    - "Bulk select operations complete within 30 seconds for 500+ items"
    - "Dashboard refreshes data within 10 seconds of changes"
    - "All dates display in user's configured timezone consistently"
    - "Pagination state persists when switching between views"
  artifacts:
    - path: frontend/src/App.js
      provides: PatchVelocityChart with data windowing for large datasets
      min_lines: 15
    - path: backend/api/hosts_v2.py
      provides: Bulk action endpoint with batched processing
      min_lines: 30
    - path: backend/api/dashboard.py
      provides: Dashboard summary with optimized queries
      min_lines: 20
  key_links:
    - from: DashboardOpsPage.jsx
      to: /api/dashboard/summary
      via: apiFetch in useCallback
      pattern: "fetchSummary"
    - from: HostsOpsPage.jsx
      to: /api/hosts/bulk
      via: executeBulk function
      pattern: "apiFetch.*bulk"
---

<objective>
Fix 5 UI/UX issues: chart rendering with large datasets, bulk select timeout, real-time updates, timezone handling, and pagination persistence.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Phase 3 addresses remaining UI issues from Phase 2 review:
- UI-004: Charts fail to render with large datasets
- UI-005: Bulk select operation times out on 500+ items  
- UI-006: Dashboard doesn't update in real-time
- UI-008: Inconsistent timezone display
- UI-010: Pagination resets when changing views

Key files identified:
- frontend/src/App.js: Contains PatchVelocityChart (lines 1004-1021)
- backend/api/hosts_v2.py: Contains bulk_action endpoint (lines 451-519)
- backend/api/dashboard.py: Dashboard summary API
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UI-004 Chart Rendering with Large Datasets</name>
  <files>frontend/src/App.js</files>
  <action>
    Modify PatchVelocityChart component to add data windowing:
    1. Add maxDataPoints prop (default 30) to cap data visualization
    2. If data.length > maxDataPoints, slice to last N points for display
    3. Add error boundary with try/catch around chart rendering
    4. Memoize chart calculations with useMemo to avoid re-computation
    
    Changes to PatchVelocityChart function (around line 1004):
    - Add `const maxDataPoints = 30` to cap displayed points
    - Add `const displayData = data?.slice(-maxDataPoints) || []` to window data
    - Replace `data.map` with `displayData.map`
    - Wrap in React.memo or add useMemo for calculations
  </action>
  <verify>node -e "require('fs').readFileSync('frontend/src/App.js','utf8').includes('slice(-maxDataPoints)')"</verify>
  <done>PatchVelocityChart renders max 30 data points, prevents browser freeze on 1000+ data points</done>
</task>

<task type="auto">
  <name>Task 2: Fix UI-005 Bulk Select Timeout</name>
  <files>backend/api/hosts_v2.py</files>
  <action>
    Optimize bulk_action endpoint for large datasets:
    1. Add batch processing - process hosts in chunks of 50 instead of all at once
    2. Add asyncio.sleep(0) between batches to yield event loop
    3. Replace selectinload with lazy loading for groups/tags (only load when action needs them)
    4. Add streaming response or progress indicator for long operations
    5. Add timeout handling with explicit commit after each batch
    
    Changes to bulk_action function:
    - Add BATCH_SIZE = 50 constant
    - Add conditional loading: only selectinload groups when action == 'set_group'
    - Add batch loop: for i in range(0, len(hosts), BATCH_SIZE): batch = hosts[i:i+BATCH_SIZE]
    - Add await asyncio.sleep(0) after each batch commit
  </action>
  <verify>python -c "import ast; ast.parse(open('backend/api/hosts_v2.py').read())"</verify>
  <done>Bulk action completes within 30 seconds for 500 hosts, with progress logging</done>
</task>

<task type="auto">
  <name>Task 3: Fix UI-006 Real-time Updates</name>
  <files>backend/api/dashboard.py, frontend/src/DashboardOpsPage.jsx</files>
  <action>
    Improve dashboard real-time refresh:
    1. In DashboardOpsPage.jsx: Reduce useInterval from 60000ms to 10000ms (10 seconds)
    2. Add conditional fetch - only refetch if tab is visible (document.hidden check)
    3. In dashboard.py: Add lightweight /health-ring endpoint that returns just critical metrics
    4. Use the lightweight endpoint for frequent polls to reduce DB load
    
    Changes:
    - DashboardOpsPage.jsx line 38: change useInterval(fetchSummary, 60000) to useInterval(fetchSummary, 10000)
    - Add useEffect with visibility check before polling
    - dashboard.py: enhance health-ring endpoint (already exists at line 117)
  </action>
  <verify>grep -n "useInterval.*10000" frontend/src/DashboardOpsPage.jsx</verify>
  <done>Dashboard data updates within 10 seconds of server changes</done>
</task>

<task type="auto">
  <name>Task 4: Fix UI-008 Timezone Handling</name>
  <files>frontend/src/App.js</files>
  <action>
    Add centralized timezone utility and apply consistently:
    1. Add TIMEZONE constant at top of App.js (default: Intl.DateTimeFormat().resolvedOptions().timeZone)
    2. Create formatDateTime() helper that formats dates with timezone
    3. Add to all date rendering: replace new Date(x).toLocaleString() with formatDateTime(x)
    
    Add after imports (around line 5):
    ```
    // Timezone utility
    const USER_TIMEZONE = Intl.DateTimeFormat().resolvedOptions().timeZone;
    const formatDateTime = (isoString) => {
      if (!isoString) return 'N/A';
      try {
        return new Date(isoString).toLocaleString(undefined, { 
          timeZone: USER_TIMEZONE,
          year: 'numeric', month: 'short', day: 'numeric',
          hour: '2-digit', minute: '2-digit'
        });
      } catch { return 'Invalid'; }
    };
    ```
    
    Update DashboardOpsPage.jsx line 326: replace last_heartbeat formatting with formatDateTime
    Update AnalyticsOpsPage.jsx line 312: same replacement
  </action>
  <verify>grep -n "formatDateTime\|timeZone" frontend/src/App.js | head -5</verify>
  <done>All dates display in user's local timezone consistently</done>
</task>

<task type="auto">
  <name>Task 5: Fix UI-010 Pagination State Persistence</name>
  <files>frontend/src/HostsOpsPage.jsx, frontend/src/CVEOpsPage.jsx</files>
  <action>
    Persist pagination state using localStorage:
    1. Add useEffect to load saved pagination on mount: savedPage = localStorage.getItem('pm_hosts_page')
    2. Add useEffect to save pagination on change: localStorage.setItem('pm_hosts_page', page)
    3. Same for CVE page (pm_cve_page)
    4. Only persist page number, not full state
    
    In HostsOpsPage.jsx:
    - After useState(page, setPage) add:
      ```
      const [page, setPage] = useState(() => {
        const saved = localStorage.getItem('pm_hosts_page');
        return saved ? parseInt(saved, 10) : 1;
      });
      ```
    - Add useEffect to persist:
      ```
      useEffect(() => {
        localStorage.setItem('pm_hosts_page', String(page));
      }, [page]);
      ```
    
    Same pattern for CVEOpsPage.jsx with 'pm_cve_page' key.
  </action>
<verify>grep -n "localStorage.*pm_hosts_page" frontend/src/HostsOpsPage.jsx</verify>
  <done>Pagination state persists when user navigates away and returns</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input (host_ids array) from bulk action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | bulk_action endpoint | mitigate | Validate host_ids not empty, limit max to 1000 |
| T-03-02 | DoS | bulk_action batch processing | mitigate | Add max batch limit, timeout after 60s |
| T-03-03 | Info Disclosure | dashboard API | accept | Already requires auth, returns only aggregate data |
</threat_model>

<verification>
Run: node -e "require('fs').readFileSync('frontend/src/App.js','utf8').includes('slice(-maxDataPoints)')"
Check bulk action optimization compiles
Verify useInterval is 10000ms in DashboardOpsPage.jsx
Confirm formatDateTime exists in App.js
Check localStorage persistence in HostsOpsPage.jsx
</verification>

<success_criteria>
All 5 issues addressed with specific fixes:
- UI-004: Chart data windowing implemented
- UI-005: Bulk action batch processing added
- UI-006: Dashboard refresh reduced to 10 seconds
- UI-008: Timezone utility created and applied
- UI-010: Pagination localStorage persistence added
</success_criteria>

<output>
After completion, create .planning/03/03-01-SUMMARY.md
</output>